# Production Environment Configuration Example
# Copy this to .env in Vercel environment variables

# Server Configuration
PORT=5000
NODE_ENV=production

# Database - Use your production database URL
DATABASE_URL="your-production-database-url"

# JWT Secret (IMPORTANT: Generate a strong secret key for production)
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRES_IN=7d

# CORS - Multiple origins (comma-separated for local dev + production)
# Update this with your actual Vercel frontend URL after deployment
CORS_ORIGIN=http://localhost:5173,https://your-frontend-app.vercel.app

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
